<form method="get" action="{{ action }}" enctype="application/x-www-form-urlencoded">

    <div class="form-group">

        {% if fields|length > 1 %}
            <label class="w-25">
                <select class="form-control" name="{{ filterFieldName }}">
                    {% for field, label in fields %}
                        <option value="{{ field }}"{% if selectedField == field %} selected="selected"{% endif %}>{{ label }}</option>
                    {% endfor %}
                </select>
            </label>
        {% else %}
            <input type="hidden" name="{{ filterFieldName }}" value="{{ fields|keys|first }}" />
        {% endif %}

        <label class="w-100">
            <input class="form-control" type="search" value="{{ selectedValue }}" name="{{ filterValueName }}" placeholder="{{ 'Ввод'|trans({}, 'KnpPaginatorBundle') }}" />
        </label>

    </div>

    <div class="form-group">
        <button class="default-link-btn">Применить</button>
    </div>

</form>
